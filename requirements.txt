# Prioritize langchain first
langchain==0.1.20  # Try 0.1.0 or 0.1.20, check PyPI for recent stable
langchain-google-generativeai

# Other dependencies (ensure they are on separate lines)
beautifulsoup4
markdownify
fastapi
uvicorn
python-dotenv
pinecone-client
langchain-pinecone